{
  "Array Input Boilerplate": {
    "prefix": "arrboiler", 
    "body": [
      "#include <iostream>",
      "using namespace std;",
      "",
      "int main() {",
      "    int m, n;",
      "",
      "    // First array",
      "    cout << \"Enter size of first array: \";",
      "    cin >> m;",
      "    int arr1[m];  ",
      "    cout << \"Enter elements of first array: \";",
      "    for (int i = 0; i < m; i++) cin >> arr1[i];",
      "",
      "    // Second array",
      "    cout << \"Enter size of second array: \";",
      "    cin >> n;",
      "    int arr2[n];",
      "    cout << \"Enter elements of second array: \";",
      "    for (int i = 0; i < n; i++) cin >> arr2[i];",
      "",
      "    // Printing arrays",
      "    cout << \"First array: \";",
      "    for (int i = 0; i < m; i++) cout << arr1[i] << \" \";",
      "    cout << endl;",
      "",
      "    cout << \"Second array: \";",
      "    for (int i = 0; i < n; i++) cout << arr2[i] << \" \";",
      "    cout << endl;",
      "",
      "    return 0;",
      "}"
    ],
    "description": "Boilerplate code for two arrays input and output"
  }
}
